{
  "$comment": "Machine-readable Product Foundation checkpoint. Regenerated by hand after each release phase, and asserted by tests/release-baseline.test.ts so it cannot drift from the code it describes. See docs/product-foundation-handoff.md.",
  "productFoundation": "Phases 5.1–5.10",
  "phase": "5.10",
  "branch": "main",
  "validatedAgainst": "3281cce2f430bbcf470af1303effb4e937ef94ed",
  "recordedByCommitMessage": "chore: complete product foundation handoff",
  "$commitNote": "The SHA of the commit that introduces this file cannot be written into the file. Resolve it with: git log --grep='chore: complete product foundation handoff' --format=%H",
  "gates": {
    "format": "pass",
    "typecheckBackend": "pass",
    "typecheckFrontend": "pass",
    "unitAndIntegration": { "files": 52, "tests": 927, "status": "pass" },
    "browserE2E": { "files": 1, "tests": 27, "status": "pass" },
    "buildBackend": "pass",
    "buildWeb": "pass",
    "desktopVerify": "pass",
    "audit": { "all": 0, "production": 0 }
  },
  "responsiveMatrix": {
    "pages": 14,
    "widths": [1920, 1440, 1024, 768, 430, 390, 375],
    "horizontalOverflow": 0,
    "touchTargetsBelowMinimum": 0,
    "clippedTextElements": 0
  },
  "publicDemo": {
    "deployed": false,
    "reason": "No public deployment exists. No domain, no DNS record, no certificate and no host were configured in Phases 5.1–5.10, so public-demo validation was not performed and is not claimed."
  },
  "signing": {
    "$comment": "Phase 6.5. Asserted against src/desktop/signing.ts by tests/release-baseline.test.ts, so this file cannot claim a signed production release while that module refuses one.",
    "updatePublicKey": "placeholder",
    "updateEndpointsReachable": false,
    "productionReleaseSigned": false,
    "reason": "The updater public key in src-tauri/tauri.conf.json is still the placeholder and its endpoint uses the reserved .invalid TLD, so no production release can be produced from this tree and none is claimed. The key itself is not in the repository and must never be: it belongs in a CI secret or on the release machine."
  },
  "safety": {
    "liveTradingEnabled": false,
    "brokerExecutionEnabled": false,
    "autonomousOrderPlacement": false,
    "liveTradingBoundary": "no operation, tool, job kind or configuration key exists for either"
  }
}
