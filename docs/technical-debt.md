# Technical debt register

Items **actually remaining** after Phases 5.1–5.10, with what each one costs and what has to happen
for it to matter.

This register deliberately holds debt, not roadmap. A capability the product intends to build later
is a plan, and plans live in [`risks-and-deferred.md`](./risks-and-deferred.md) and the phase docs. An
entry appears here only when the current state is _already_ worse than it should be, or when
something shipped cannot yet carry the weight it is expected to. Where a genuine deferral has a
trigger — the security and privacy items, TLS termination, the `pg` driver — it is listed in
`risks-and-deferred.md` §6 rather than duplicated here.

`Blocking` means: a next phase that depends on this must do it first. `Non-blocking` means the
Foundation works, and the cost is real but bounded.

| Id     | Area                  | Description                                                                                                                                                                                                                                                                                                    | Severity | Impact                                                                                                                           | Workaround                                                                                                                                                     | Recommended phase                                                                          | Blocking                                 |
| ------ | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------- |
| TDR-1  | Privacy / data rights | Retention is `by-user-request` but no export or hard-delete surface exists. The credit ledger, context versions and audit records are append-only by design, so erasure needs a documented exception rather than a delete.                                                                                     | High     | A user cannot exercise a data-subject request against a running system                                                           | None. Read the append-only rationale in `security-and-privacy.md` §14 before promising deletion to anyone                                                      | Before accepting real user data                                                            | **Blocking** for real users              |
| TDR-2  | User context          | `derivedField()` exists and `derived` is a first-class status, but nothing writes one; and conflict findings are re-derived on each read instead of being recorded as having been asked.                                                                                                                       | Low      | A capability that needs a computed context value cannot store one, and the Agent cannot tell whether it already asked a question | Recompute on read; the cost is correctness-neutral, only wasteful                                                                                              | With the first capability that computes a context value                                    | Non-blocking                             |
| TDR-3  | Credits / metering    | No payment provider and no billing integration — deliberate for Phase 5.4 — and metering has no durable queue. A credit ledger exists; a subscription lifecycle is not yet real.                                                                                                                               | Medium   | No revenue path, and metering cannot survive a multi-process deployment                                                          | Single process; the balance still bounds spend                                                                                                                 | With the billing and compliance review                                                     | Non-blocking until billing               |
| TDR-4  | Market data           | No market-data provider exists with provenance, so portfolio and evaluation rest on **declared** prices, and four priced capabilities (`portfolio.analysis`, `research.report`, `backtest.run`, `dataset.process`) are `coming-soon`.                                                                          | High     | The two priority systems of the product (portfolio intelligence, decision evaluation) cannot rest on observed data               | Declared prices with explicit provenance and freshness; low quality narrows readiness rather than being hidden                                                 | Next product phase                                                                         | **Blocking** for real analysis           |
| TDR-5  | Agent / provider      | No hosted provider is registered — the default adapter is offline and scripted — so nothing has rendered real model output, and the AI Workspace remains a labelled prototype. Capability runs are audited but not persisted as records.                                                                       | High     | The interface has never been exercised against a real model turn, so its structured-result path is proved only by fixtures       | The offline adapter is a working default; the pipeline is covered by tests                                                                                     | Next product phase                                                                         | **Blocking** for a demo of real analysis |
| TDR-6  | Compliance            | Seven items in `security-and-privacy.md` §14 require professional review: advice characterization per jurisdiction, financial-data handling, data-subject rights against append-only records, consent and terms, deployment security, marketing claims and provider data-processing agreements.                | High     | No legal conclusion is or can be asserted by a test                                                                              | None; the docs mark each item as requiring review rather than answering it                                                                                     | Before any public or beta release                                                          | **Blocking** before production           |
| TDR-7  | Frontend bundle       | A single ~1.0 MB frontend chunk with no code splitting. Harmless inside the Tauri shell, which loads from local disk.                                                                                                                                                                                          | Low      | First paint on a network-served build would be slow                                                                              | None needed while the shell serves from disk; measured at build time                                                                                           | With the first browser-hosted deployment                                                   | Non-blocking                             |
| TDR-8  | Test coverage         | The browser suite measures geometry, not appearance: no pixel-diff baselines exist, so a colour or type-scale regression that keeps the layout would pass. Contrast was not re-measured in Phase 5.10.                                                                                                         | Medium   | A visual regression can reach a release unnoticed by the suite                                                                   | Layout probes, screenshot viewport dimensions and an element-count floor; human review of a page                                                               | When a design system is locked for release                                                 | Non-blocking                             |
| TDR-9  | Test reliability      | Coverage stays environment-conditional: the browser suite skips by name without an installed browser, and the database, bundle and Tailwind-native checks remain guarded. A green run on a bare machine ran fewer tests.                                                                                       | Medium   | "Green" is not a fixed quantity unless the environment is pinned                                                                 | vitest reports guarded tests as skipped rather than passed; CI should pin a browser and the full toolchain                                                     | With CI that runs the release gate                                                         | Non-blocking                             |
| TDR-10 | Test tooling          | The browser suite drives the DevTools Protocol directly — ~250 lines to maintain, against a protocol that is stable but not a public API in the way a test framework is.                                                                                                                                       | Low      | A Chrome protocol change would need a fix in `tests/browser/driver.ts`                                                           | The surface used is small and long-stable: `Page`, `Runtime`, `Emulation` and `Input`                                                                          | Reassess if the driver needs a third capability                                            | Non-blocking                             |
| TDR-11 | Realtime / jobs       | The queue is per process, so a multi-process deployment needs a durable queue; and there is no purge job acting on `observability.auditRetentionDays`.                                                                                                                                                         | Medium   | Logs and job state do not survive a lost process, and retention is declared but not enforced                                     | Single process; the dead-letter store is bounded                                                                                                               | Before a multi-process or hosted deployment                                                | Non-blocking until hosting               |
| TDR-12 | Desktop runtime       | Windows has no `SIGTERM`: `std::process::Child::kill` there is `TerminateProcess` and `std` offers nothing else, so `sidecar::terminate` can only send a real graceful signal on Unix. The API is therefore _terminated_ rather than asked on Windows.                                                         | Medium   | SQLite may need WAL recovery after a Windows shutdown, which is precisely the cost the graceful path exists to avoid             | WAL makes a hard stop survivable; the forced path is recorded in the process report's `lastError` rather than hidden                                           | If the API gains an out-of-band shutdown request (a control socket), or with 6.6 hardening | Non-blocking                             |
| TDR-13 | Desktop runtime       | The Rust supervisor is never compiled or executed in this environment — there is no toolchain. `process-state.agreement`, `process.policy-agreement` and `protocol.agreement` prove the two implementations _describe_ one machine with one set of deadlines, and nothing more.                                | Medium   | A defect in `sidecar.rs` that keeps the same names and numbers would pass every check here and only appear in a packaged build   | The Node supervisor, exercised against real child processes, tests the behaviour the Rust side mirrors; the verifier lists this in `unverifiable`              | With CI that has the Rust toolchain, or at 6.6 release QA                                  | Non-blocking                             |
| TDR-14 | Desktop storage       | Local database and stored file content are **not encrypted at rest**. `storage.allowSensitiveFiles` is `false`, so the policy refuses sensitive categories outright, but anything stored is readable by whatever can read the app-data directory. Phase 6.3 built the layout; it did not build key management. | High     | On a shared machine or a stolen disk, a learner's stored documents and their training history are readable without the app       | OS filesystem permissions; the sensitive-file refusal keeps the most sensitive categories out entirely                                                         | 6.4 — secure storage and key management                                                    | **Blocking** before real user data       |
| TDR-15 | Desktop storage       | Orphaned content — a blob with no `files` row, left by an interrupted delete — is reclaimed only when `reconcile({ removeOrphans: true })` is called. There is no scheduled sweep and no per-owner quota enforcement above the policy's total-bytes ceiling.                                                   | Low      | Storage grows by the size of interrupted deletes, slowly, and a single owner can consume the shared ceiling                      | `verifyIntegrity()` reports orphans without deleting, so the state is visible and the sweep is one call; content deduplication keeps the common case near zero | With a settings/maintenance surface, or at 6.6 hardening                                   | Non-blocking                             |

## What is deliberately **not** in this register

- **Deferred security work with a known trigger** (TLS termination, trusted-proxy placement, per-route
  rate-limit tiers, audit retention enforcement, incident runbook). Listed in `risks-and-deferred.md`
  §6, because each is correctly deferred rather than accidentally missing.
- **Unbuilt capabilities that are priced and declared** (`backtest.run`, `research.report`,
  `market.structure`, `portfolio.risk`, `memory.export`). These are declarations, not debt: the
  registry says `coming-soon` or `disabled` on screen, and the boot check refuses a build that names
  an unknown analysis type, feature, cost or operation.
- **The prototype interface itself.** Rendering labelled fixtures is the declared scope of Phases
  5.1–5.10 and is stated on screen (`Preview · mock data`), tested, and recorded as risk R18. Debt
  would be rendering mock data _without_ saying so.
