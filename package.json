{
  "name": "master-trade",
  "version": "0.2.0",
  "description": "Master Trade — Trading Training Agent: Model + typed deterministic Tools + versioned Instructions. Training-only, no live trading.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.build.json",
    "validate": "npm run format:check && npm run typecheck && npm run test && npm run build",
    "agent:demo": "node dist/agent/demo.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
